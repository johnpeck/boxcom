\asciicmd{\$setser\{i\}}{
    \index{internal_cmds_index}{\$setser}
    \label{cmd:setser}

    \textbf{Name:} \texttt{\$setser} -- Set the serial number.\\
    \textbf{Description:} Use \texttt{\$setser} to set the
    instrument's serial number during calibration.  Use the
    \texttt{*idn?} query described \vpageref{cmd:idn} to query the serial number.

}